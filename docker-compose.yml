redis:
  image: redis:latest
  command: redis-server --appendonly yes

web:
  image: python:2-onbuild
  environment:
    - REDIS_HOST=redis
  env_file: .env
  links:
    - redis:redis
  ports:
    - 5000
  command: gunicorn -b 0.0.0.0:5000 app
